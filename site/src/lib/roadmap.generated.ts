// GENERATED by scripts/roadmap.mjs from docs/ROADMAP.md, through roadkeep. Do not edit.
// `npm run generate` rewrites this file; `npm test` fails if it drifted from the roadmap.

export interface GeneratedBlock {
  /** the single letter the roadmap files it under */
  block: string;
  title: string;
  /** lines still open in that block */
  open: number;
}

export interface GeneratedTask {
  id: string;
  block: string;
  /** the failure the line exists to remove, in the roadmap's own words */
  symptom: string;
  /** the measurement or the argument behind it */
  why: string;
  deps: string[];
}

export interface GeneratedNonGoal {
  lead: string;
  why: string;
}

export const generatedBlocks: GeneratedBlock[] = [
  { block: "A", title: "What a tool call costs the turn", open: 0 },
  { block: "B", title: "Seeing the result cheaply", open: 2 },
  { block: "C", title: "The asset compiler", open: 0 },
  { block: "D", title: "Fetching from a paid service without surprise", open: 0 },
  { block: "E", title: "One world with the engine", open: 2 },
  { block: "F", title: "Motion", open: 0 },
  { block: "G", title: "Geometry as a declaration", open: 0 },
  { block: "H", title: "Proof on a real game", open: 6 },
  { block: "I", title: "Voxel models from a declaration", open: 0 },
  { block: "J", title: "A bar a person sets once", open: 0 },
  { block: "K", title: "Reached without reading the source", open: 11 },
  { block: "L", title: "What a run leaves as evidence", open: 7 },
  { block: "M", title: "What a game needs beyond the look", open: 3 },
];

export const generatedTasks: GeneratedTask[] = [
  {
    id: "PW141",
    block: "B",
    symptom: "a bake does not say whether its silhouette moved or only its look, so two bakes are compared by measuring both again",
    why: "An agent asking \"did my bevel change the outline\" re-runs the measures on both pictures, when one string per question would answer it across sessions.",
    deps: [],
  },
  {
    id: "PW145",
    block: "B",
    symptom: "a bake after `preview_size` changed is served the old-size picture from the cache, as a hit",
    why: "The rung's size is read from the project file and is in no field the cache key covers, so every measure on the hit answers for a size nobody asked for.",
    deps: [],
  },
  {
    id: "PW118",
    block: "E",
    symptom: "a capture that changes blames an undeclared setting when what moved was a file the game loaded",
    why: "After the stars moved all four Cottony captures raised the alarm, and finding the cause took a script diffing screenshots, because a capture records no inputs at all.",
    deps: [],
  },
  {
    id: "PW119",
    block: "E",
    symptom: "nothing answers which captures and renders depend on a file, so a changed sprite means re-taking all of them",
    why: "Four captures were re-taken and diffed to learn which showed a star; a project with forty cannot, and one that re-takes too few commits a stale screenshot.",
    deps: ["PW118"],
  },
  {
    id: "PW57",
    block: "H",
    symptom: "every Cottony look gate restates its own floor, so no asset has a bar a search can aim at",
    why: "Five check scripts measure after the render is spent, which is a verdict and never a target, and a threshold moved in one of them is invisible to the rest.",
    deps: ["PW53"],
  },
  {
    id: "PW59",
    block: "H",
    symptom: "nothing says how much of Cottony still does its own version of what the plugin does",
    why: "Adoption is asserted per asset, so a hand-rolled rig, gate or runner can come back without anything failing, and 100% stays an opinion.",
    deps: ["PW53", "PW56", "PW57"],
  },
  {
    id: "PW56",
    block: "H",
    symptom: "twelve GDScript runners in Cottony each start Godot their own way, and a settings mismatch is silent",
    why: "The seven perf scripts started by hand have no driver, and five of them are gates whose verdict shape is §PW57's question.",
    deps: ["PW57"],
  },
  {
    id: "PW116",
    block: "H",
    symptom: "a ledger side that never measured the hand work still yields a conclusive verdict",
    why: "Every before side says in prose that the hand search was never timed, and `compare` sums it as complete and calls the stars not reduced.",
    deps: [],
  },
  {
    id: "PW117",
    block: "H",
    symptom: "every symptom in the backlog was measured on one game, so the config boundary is tested by a single adopter",
    why: "A plate at CELL, a placed covers and lights scaled for two star sizes are general in form and used by nobody else, so Cottony's shape may already be compiled in.",
    deps: [],
  },
  {
    id: "PW120",
    block: "H",
    symptom: "every family port is a hand-written script repeating the same build, search, check, bake and record steps",
    why: "Three Cottony port scripts are one skeleton with different filling, and five more families would be five more copies, each with its own small mistakes.",
    deps: [],
  },
  {
    id: "PW123",
    block: "K",
    symptom: "`build --all` skips a declaration it refuses to read, so a typo drops an asset from the build and exits 0",
    why: "The loop swallows every refusal so it can pass over TOML that is not a shape, and an unknown field is exactly such a refusal.",
    deps: [],
  },
  {
    id: "PW124",
    block: "K",
    symptom: "`describe` knows one operation, so search, accept, geometry and voxels are still learned by reading their source",
    why: "PW3 shipped self-description and only `render.bake` took it up; every operation since arrived as a plain function, and no test notices one more.",
    deps: [],
  },
  {
    id: "PW125",
    block: "K",
    symptom: "an agent in a consumer can ask what the machine can do, explain a code or start a job only by writing Python",
    why: "The command line has two verbs, `build` and `verify`, so a GDScript project writes a script to reach the rest, which is the port scripts' cost at its smallest.",
    deps: ["PW124"],
  },
  {
    id: "PW126",
    block: "K",
    symptom: "an agent's first call is guessed from prose, because no tool schema carries the plugin's names, ranges and choices",
    why: "A range declared on a parameter reaches the caller only as a refusal, where a served schema would have stopped the wrong call before it spawned anything.",
    deps: ["PW125"],
  },
  {
    id: "PW127",
    block: "K",
    symptom: "a remedy is a sentence, so nothing checks that the call it names exists or accepts those arguments",
    why: "Roadkeep found one of 118 remedy rows had ever been run and several named flags its own parser rejected, and remedies here are the same unchecked prose.",
    deps: ["PW125"],
  },
  {
    id: "PW128",
    block: "K",
    symptom: "a refused name says what was wrong but never which names would have worked",
    why: "An unknown measure, primitive, material slot or operation is where an agent guesses, and the near match exists only inside a sentence the config reader writes.",
    deps: [],
  },
  {
    id: "PW129",
    block: "K",
    symptom: "nothing bounds what `describe`, `capabilities` or an error costs the turn that reads it",
    why: "The reads an agent makes every session have no ceiling, so each operation registered makes them longer and nothing measures by how much.",
    deps: ["PW124"],
  },
  {
    id: "PW130",
    block: "K",
    symptom: "every test calls the surface correctly, so a silently dropped argument or a wrong first call is never measured",
    why: "Shio's naive client found six silent drops in twelve calls that its benchmark, driving a correct script, could not see.",
    deps: ["PW125"],
  },
  {
    id: "PW131",
    block: "K",
    symptom: "starting work on an asset means opening its declaration, spec, record and last verdict one file at a time",
    why: "An asset's state is split across four files, so an agent opens each before it can say what is left, and no read answers that question whole.",
    deps: ["PW125"],
  },
  {
    id: "PW132",
    block: "K",
    symptom: "an agent in a consumer is never told the plugin exists, because it ships no manifest, skill or session notice",
    why: "Six of Cottony's tools import the plugin and not one of its agent documents names it, so each session rediscovers it from a script's imports.",
    deps: [],
  },
  {
    id: "PW133",
    block: "K",
    symptom: "an agent may hand-edit a built mesh or voxel file, and nothing notices the output no longer follows its declaration",
    why: "The build stamp hashes inputs only and geometry outputs carry no record, so an edited output is reported cached and rebuilt only when an input moves.",
    deps: ["PW125"],
  },
  {
    id: "PW134",
    block: "L",
    symptom: "a green suite with Blender and Godot absent reads the same as one that rendered, and leaves no record it ran",
    why: "The render, engine and capture tests skip without `bpy` or `$GODOT`, and a backgrounded run is known only by whoever read the tail of its output.",
    deps: [],
  },
  {
    id: "PW135",
    block: "L",
    symptom: "a test log written at the repository root is staged by the commit tool, because nothing ignores `*.log`",
    why: "`run-commit.cmd` stages everything, which is how Shio committed sh545.log with a fix, and a five-minute suite is exactly the run an agent redirects to a file.",
    deps: [],
  },
  {
    id: "PW136",
    block: "L",
    symptom: "no pytest or ruff runs anywhere but a desk, so a consumer pinned to a commit gets one nobody gated",
    why: "The workflows run only roadkeep's lint and the site, while Cottony's CI installs this repository at a pinned commit.",
    deps: ["PW134"],
  },
  {
    id: "PW137",
    block: "L",
    symptom: "the site's own test requires the page to say there is no implementation, after a hundred lines have shipped",
    why: "README and CLAUDE.md still say Block A has started, so the first thing a reader or an agent learns about the plugin is false, and a test defends it.",
    deps: [],
  },
  {
    id: "PW138",
    block: "L",
    symptom: "the tool-surface spec names twelve error areas and the code declares nineteen, and nothing reads one against the other",
    why: "A name a spec spells that the code lacks, or the reverse, is found only by a reader who checks both, which is the drift the specs exist to prevent.",
    deps: [],
  },
  {
    id: "PW139",
    block: "L",
    symptom: "two jobs started at once can both pass the capacity check, so `[render] max_parallel` is exceeded",
    why: "`start` lists the running jobs and then spawns with nothing held between the two, the shape of roadkeep's race that minted one id twice.",
    deps: [],
  },
  {
    id: "PW140",
    block: "L",
    symptom: "a build is reported cached after the plugin that built it changed, because its stamp hashes the inputs alone",
    why: "The stamp leaves out the plugin's version and `--preview`, and geometry writes no provenance record, so two answers to \"what made this\" already disagree.",
    deps: [],
  },
  {
    id: "PW142",
    block: "M",
    symptom: "a voxel model is drawn with every filled cell, including the ones buried where no face of them can be seen",
    why: "`multimesh()` instances every centre while the same file records each cell's depth, so a solid model pays for its whole volume and not its skin.",
    deps: [],
  },
  {
    id: "PW143",
    block: "M",
    symptom: "an acceptance spec bounds how an asset looks and never what it costs the game to draw",
    why: "A search that passes every look predicate may triple the triangles, materials or texture memory, and nothing in the spec would refuse the rig that did it.",
    deps: [],
  },
  {
    id: "PW144",
    block: "M",
    symptom: "the rig a search found is kept nowhere, so each run searches it again and no other game can start from it",
    why: "`search_stars.py` spends up to sixty renders finding the stars' rig again, and the half it holds fixed is constants in the script, where no other family can reuse it.",
    deps: [],
  },
];

export const generatedNonGoals: GeneratedNonGoal[] = [
  {
    lead: "A graphical editor",
    why: "The caller here is an agent in a terminal, so a surface that needs a person at a screen to operate is one the agent cannot use at all.",
  },
  {
    lead: "Replacing Blender, Godot or the generative service",
    why: "Those three already do the work; what is missing is the loop around them, so this orchestrates and measures and never re-implements a renderer, an engine or a mesh generator.",
  },
  {
    lead: "Spending money on the agent's own judgement",
    why: "A fetch draws on a real balance, so the ceiling is a person's to set and the plugin's job is to make a spend bounded and visible, never to decide one is worth it.",
  },
  {
    lead: "One project's palette, rig or paths compiled in",
    why: "Cottony is the first consumer and not the specification, so anything it needs that a second project would not is configuration, and a default that cannot be overridden is a defect.",
  },
  {
    lead: "A hosted service or an account to sign into",
    why: "Everything runs on the developer's machine against files in their own repository, because a plugin that needs an account is one that fails on the day the account does.",
  },
  {
    lead: "An agent accepting or rejecting its own look",
    why: "A look is what a person agreed to, so an agent that can write the verdict on its own run turns the one check the loop cannot automate into one more number it tunes.",
  },
];

/** Lines set aside rather than shipped. They keep their ids, and are still waited on. */
export const generatedPaused: string[] = ["PW36", "PW53", "PW77", "PW78", "PW79", "PW80", "PW81", "PW82", "PW113"];

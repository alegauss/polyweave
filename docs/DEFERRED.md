# Set aside

## Block A — What a tool call costs the turn

## Block B — Seeing the result cheaply

## Block C — The asset compiler

## Block D — Fetching from a paid service without surprise

## Block E — One world with the engine

## Block F — Motion

## Block G — Geometry as a declaration

## Block H — Proof on a real game

- ⏸ **PW36** (deps: PW5 ✅) **a real project cannot adopt the plugin without carrying its own paths and palette into it** — set aside (Nobody has recorded that baseline yet.): Porting the pipeline edits another repository, and cannot start before a before-side baseline is recorded for the asset that moves first. → §PW36
- ⏸ **PW53** (deps: PW36 ✅) **Cottony lights twelve models through fourteen constants of its own, and the search never sees one of them** — set aside (a person's judgement): One asset moving leaves bake_model.py standing for the other eleven, so the rig the plugin exists to remove is still the thing that runs. → §PW53
